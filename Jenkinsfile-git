pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        withCredentials([gitUsernamePassword(credentialsId: 'git_cred', variable: 'GIT_CREDENTIALS')]) {
          sh 'python pull_and_rmdir2.py'
        }
      }
    }
  }
}